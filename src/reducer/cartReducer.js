function cartReducer(cart, action){
    switch(action.type){
        case "increment":{

        }
        case "decrement":{
            
        }
        default:
            return "Unknown method"
    }
}